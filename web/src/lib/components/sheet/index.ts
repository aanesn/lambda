import { Dialog } from "bits-ui"
import Content from "./sheet-content.svelte"
import Header from "./sheet-header.svelte"

const Root = Dialog.Root
const Trigger = Dialog.Trigger

export { Root, Content, Trigger, Header }
